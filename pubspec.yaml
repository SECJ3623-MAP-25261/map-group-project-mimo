name: profile_managemenr
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0        # Required for all Firebase services
  firebase_auth: ^4.17.0        # For authentication
  cloud_firestore: ^4.15.0      # If you store user data (e.g., name, phone)


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/widgets/CampusClosetLogo.png
    - lib/widgets/Mail gemini.png